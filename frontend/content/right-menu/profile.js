import { state } from "../../model.js";

export const profileView = {
	profile: `<div class="center">
	<h2 class="no-pbm" style="pointer-events: none;user-select: none;">${console.log(state)}</h2>
</div>`
};
