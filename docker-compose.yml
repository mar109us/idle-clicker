services:
   db:
      image: postgres:16-alpine
      container_name: 55clicks_db_local
      restart: always
      env_file: .env
      ports:
         - "5432:5432"
      volumes:
         - ./pgdata:/var/lib/postgresql/data
